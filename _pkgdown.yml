url: https://httr2.r-lib.org

reference:
- title: Define a request
  contents:
  - request
  - req_body_none
  - req_error
  - req_headers
  - req_method
  - req_retry
  - req_template
  - req_throttle
  - req_timeout
  - req_url
  - req_user_agent
  - req_verbose

- title: Authenticate a request
  contents:
  - starts_with("req_auth")
  - starts_with("req_oauth")

- title: Perform a request and get a response
  contents:
  - req_dry_run
  - req_fetch
  - req_stream
  - last_request

- title: Handle the response
  contents:
  - starts_with("resp_")

- title: Miscellaenous helpers
  contents:
  - secrets
  - obfuscate


- title: OAuth flows
  desc: >
    These functions implement various OAuth flows, returning an access token.
    Generally, you should not need to call them directly, instead using the
    `req_oauth_*` helpers
  contents:
  - starts_with("oauth_")
